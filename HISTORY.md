# 변경 이력

이 문서는 HAMS Planning Board의 기능, API, 데이터 모델과 주요 버그 수정 이력을 날짜별로 누적합니다.

## 기록 규칙

- 최신 날짜를 위에 둡니다.
- 같은 날짜의 변경은 기존 날짜 아래에 추가합니다.
- 사용자 동작, API·데이터 영향, 호환성 또는 마이그레이션 필요 여부를 기록합니다.
- 기존 데이터 보정이 필요하면 별도 항목으로 명시합니다.

## 2026-08-31

### 템플릿과 프로젝트 진입

- 서비스 대시보드, 모바일 예약, 관리자 페이지 샘플을 `data/*.json`으로 추가했습니다.
- `/template-data`에서 비로그인 상태로 템플릿 JSON을 불러오도록 연결했습니다.
- 최근 작업에서 프로젝트 ID를 전달하고 `/planning`에서 저장 문서를 자동 로딩하도록 수정했습니다.
- 코멘트 목록에서 해당 프로젝트로 바로 이동하도록 링크를 수정했습니다.

### 편집기와 캔버스

- 서비스 전체 버튼과 링크에 손 모양 커서를 적용하고 비활성화 상태를 구분했습니다.
- 검토·완료·저장 모달 버튼이 작은 화면에서도 보이도록 스크롤과 고정 하단 영역을 적용했습니다.
- 빈 페이지에서 Desktop(860px), Tablet(650px), Mobile(390px) 크기를 선택하게 했습니다.
- 페이지에 하위 요소가 있으면 현재 크기를 제외한 다른 디바이스 버튼을 비활성화했습니다.
- 디바이스 변경이 선택된 페이지에만 적용되도록 수정했습니다.

### 승인 워크플로

- 소유자의 검토 요청과 직접 완료, 검토자의 반려·완료 흐름을 추가했습니다.
- 검토 이메일을 기준으로 문서 조회 권한을 자동 부여했습니다.
- 검토자는 저장 대신 반려·완료 버튼을 사용하고 처리 내용을 모달에서 입력합니다.
- `review` 상태에서는 저장·재검토·소유자 직접 완료를 UI와 API에서 차단했습니다.
- 완료 문서를 다시 저장해 `draft`로 전환하고 검토·완료를 반복할 수 있게 했습니다.
- 저장 버튼에 확인 모달을 추가했습니다.

### 승인 이력과 데이터

- 프로젝트별 `approvalHistory` 하위 컬렉션을 추가했습니다.
- 저장, 검토 요청, 반려, 소유자 직접 완료, 검토자 완료를 프로젝트 상태와 같은 batch로 기록합니다.
- 이력에 `projectId`, `projectTitle`, `ownerId`, `ownerEmail`, 상태 전이, 행위자, 검토자, 메시지, 생성 시각을 저장합니다.
- 프로젝트별 승인 이력 조회 API `GET /api/projects/{projectId}/review`를 추가했습니다.
- 여러 프로젝트 이력은 향후 `collectionGroup("approvalHistory")`로 통합 조회할 수 있습니다.
- 기존 이력에는 새 프로젝트 메타데이터가 자동 추가되지 않으므로 필요하면 백필해야 합니다.

### 문서

- 현재 구현을 기준으로 `README.md`, `AGENTS.md`, `CLAUDE.md`를 재작성했습니다.
- 이후 변경을 누적하기 위해 `HISTORY.md`를 추가했습니다.
